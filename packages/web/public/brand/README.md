Place the official brand files here:
- `logo.svg`: horizontal logo (shown in the sidebar and on the login page).
- `logo-mark.svg`: optional icon-only mark.
- `Helixona-Logo.png`: the official logo from helixona.com, once uploaded (the app switches to it via `src/brand.ts`).
The current files are provisional. The CSP only allows same-origin images, which is why they live in `public/`.
Fonts (Playfair Display, Montserrat; SIL Open Font License) are self-hosted in `../fonts/`.
